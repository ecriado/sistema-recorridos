import { Edificio, Usuario, Recorrido, Tarea, TareaAutomatica } from '../types';

// Colecciones iniciales vacías para que la aplicación refleje EXCLUSIVAMENTE
// lo que está almacenado en las tablas de Supabase (sin datos ficticios precargados).
export const initialEdificios: Edificio[] = [];

export const initialUsuarios: Usuario[] = [];

export const initialRecorridos: Recorrido[] = [];

export const initialTareas: Tarea[] = [];

export const initialAutomatizaciones: TareaAutomatica[] = [];
